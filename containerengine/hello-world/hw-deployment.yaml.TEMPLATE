apiVersion: v1
kind: Deployment
metadata:
  name: {{APP_NAME}}
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: {{APP_LABEL}}
    spec:
      containers:
      - name: {{APP_LABEL}}
        image: {{IMAGE}}
        ports:
        - containerPort: 8080
